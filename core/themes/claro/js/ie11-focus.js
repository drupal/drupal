/**
* DO NOT EDIT THIS FILE.
* See the following change record for more information,
* https://www.drupal.org/node/2815083
* @preserve
**/

(function ($) {
  Drupal.behaviors.ie11focus = {
    attach: function attach(context) {
      if (navigator.userAgent.indexOf('MSIE') !== -1 || navigator.appVersion.indexOf('Trident/') > -1) {
        $(context).find('*').once('prevent-focus-errors').on('mousedown', function (e) {
          var $target = $(e.target);

          if (!$target.is('button,input,select,textarea,a,audio,video,[tabindex],[contenteditable],iframe,embed[type="video/quicktime"],embed[type="video/mp4"],object[type="application/x-shockwave-flash"],svg:not([focusable="false"]),rect[focusable="true"]')) {
            e.preventDefault();
          }
        });
      }
    }
  };
})(jQuery);