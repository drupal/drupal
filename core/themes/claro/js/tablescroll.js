/**
* DO NOT EDIT THIS FILE.
* See the following change record for more information,
* https://www.drupal.org/node/2815083
* @preserve
**/

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); return Constructor; }

(function (Drupal, $, debounce) {
  Drupal.TableScroll = function () {
    function _class(table) {
      var _this = this;

      _classCallCheck(this, _class);

      this.table = table;
      this.wrapper = table.parentElement;
      this.wrapperOutline = this.wrapper.parentElement;
      this.stickyHeader = null;
      this.fixedTopValue = null;
      var horizontalScrollSelectors = debounce(this.applyHorizontalScrollSelectors.bind(this), 150);
      var $window = $(window);
      var $document = $(document);
      var $wrapper = $(this.wrapper);
      horizontalScrollSelectors();
      $wrapper.on('scroll.TableScroll', horizontalScrollSelectors);
      $window.on('resize.TableScroll', horizontalScrollSelectors);
      $document.on('drupalToolbarTrayChange', horizontalScrollSelectors);
      $document.on('tableheaderCreateSticky', function () {
        _this.stickyHeader = _this.wrapper.querySelector('.sticky-header');

        _this.clipFixedStickyHeader();

        $wrapper.on('scroll.TableScroll', debounce(_this.horizontalScrollHandleStickyHeader.bind(_this), 150));
        var updateStickyHeader = debounce(_this.updateStickyHeader.bind(_this), 150);
        $window.on('scroll.TableScroll resize.TableScroll', updateStickyHeader);
        $document.on('drupalToolbarTrayChange', updateStickyHeader);
      });
    }

    _createClass(_class, [{
      key: "updateStickyHeader",
      value: function updateStickyHeader() {
        if (this.stickyHeader.style.position === 'absolute') {
          this.makeStickyHeaderFixed();
        } else {
          this.clipFixedStickyHeader();
        }
      }
    }, {
      key: "makeStickyHeaderFixed",
      value: function makeStickyHeaderFixed() {
        this.stickyHeader.style.position = 'fixed';
        this.stickyHeader.style.left = "".concat(this.stickyHeader.getBoundingClientRect().left, "px");
        this.stickyHeader.style.top = "".concat(this.fixedTopValue, "px");
        this.clipFixedStickyHeader();
      }
    }, {
      key: "horizontalScrollHandleStickyHeader",
      value: function horizontalScrollHandleStickyHeader() {
        if (this.stickyHeader.style.position === 'fixed') {
          var wrapperTopOffset = this.wrapper.getBoundingClientRect().top;
          var stickyTopOffset = this.stickyHeader.getBoundingClientRect().top;
          this.fixedTopValue = stickyTopOffset;
          this.stickyHeader.style.position = 'absolute';
          this.stickyHeader.style.left = 0;
          this.stickyHeader.style.top = "".concat(Math.abs(wrapperTopOffset) + stickyTopOffset, "px");
          this.stickyHeader.style.clip = 'unset';
        }
      }
    }, {
      key: "applyHorizontalScrollSelectors",
      value: function applyHorizontalScrollSelectors() {
        var wrapperOutline = this.wrapperOutline;
        var wrapperBoundingRect = this.wrapper.getBoundingClientRect();
        var tableBoundingRect = this.table.getBoundingClientRect();
        var wrapperWidth = wrapperBoundingRect.width;
        var wrapperOffset = wrapperBoundingRect.left;
        var tableWidth = tableBoundingRect.width;
        var tableOffset = tableBoundingRect.left;
        var $wrapperOutline = $(wrapperOutline);
        $wrapperOutline.toggleClass('scrollable-table-outline--scroll', wrapperWidth < tableWidth);
        $wrapperOutline.toggleClass('scrollable-table-outline--scroll-left', wrapperOffset > tableOffset);
        $wrapperOutline.toggleClass('scrollable-table-outline--scroll-right', Math.ceil(wrapperOffset + wrapperWidth) < Math.floor(tableOffset + tableWidth));
      }
    }, {
      key: "clipFixedStickyHeader",
      value: function clipFixedStickyHeader() {
        var wrapperBoundingRect = this.wrapper.getBoundingClientRect();
        var wrapperWidth = wrapperBoundingRect.width;
        var scrollLeft = this.wrapper.scrollLeft;

        if (wrapperWidth < this.stickyHeader.clientWidth) {
          this.stickyHeader.style.clip = "rect(auto,  ".concat(wrapperWidth + scrollLeft, "px, auto, ").concat(scrollLeft - 1, "px)");
        } else {
          this.stickyHeader.style.clip = 'unset';
        }
      }
    }]);

    return _class;
  }();

  Drupal.TableScroll.tables = [];
})(Drupal, jQuery, Drupal.debounce);