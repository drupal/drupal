{#
/**
 * @file
 * Theme override for a marker for new or updated content.
 *
 * Available variables:
 * - status: Number representing the marker status to display. Use the constants
 *   below for comparison:
 *   - Drupal\node\NodeListBuilder::MARK_NEW
 *   - Drupal\node\NodeListBuilder::MARK_UPDATED
 *   - Drupal\node\NodeListBuilder::MARK_READ
 */
#}
{% if logged_in %}
  {% if status is constant('Drupal\\node\\NodeListBuilder::MARK_NEW') %}
    {{ 'New'|t }}
  {% elseif status is constant('Drupal\\node\\NodeListBuilder::MARK_UPDATED') %}
    {{ 'Updated'|t }}
  {% endif %}
{% endif %}
