langcode: en
status: true
dependencies:
  enforced:
    config:
      - system.menu.exclude-test
id: indirect-exclude-test
label: Test
description: 'Test menu depending indirectly on config_test'
locked: true
