# Schema for the configuration files of the syslog module.

syslog.settings:
  type: config_object
  label: 'Syslog settings'
  mapping:
    identity:
      type: string
      label: 'Identity'
    facility:
      type: integer
      label: 'Facility'
    format:
      type: string
      label: 'Format'
