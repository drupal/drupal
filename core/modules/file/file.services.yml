services:
  file.usage:
    class: Drupal\file\FileUsage\DatabaseFileUsageBackend
    arguments: ['@config.factory', '@database', 'file_usage', '@entity_type.manager', '@entity_field.manager']
    tags:
      - { name: backend_overridable }
