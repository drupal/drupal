# Schema for the configuration files of the Config translation module.

config_translation.settings:
  type: config_object
  label: 'Configuration translation settings'
  mapping:
    translate_source:
      type: boolean
      label: 'Enable configuration translation for source language'
